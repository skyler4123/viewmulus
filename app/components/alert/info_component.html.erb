<div data-controller="alert--info-component" class="flex flex-row justify-between p-4 mb-4 text-sm text-blue-800 rounded-lg bg-blue-50 dark:bg-gray-800 dark:text-blue-400" role="alert">
  <% if content? %>
    <%= content %>
  <% else %>
    <div><%= @label %></div>
    <%= render(Button::CloseComponent.new) if @closable %>
  <% end %>
</div>