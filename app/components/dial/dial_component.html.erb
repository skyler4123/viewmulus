<div data-controller="dial--dial-component" class="w-14 h-14 bg-sky-500 rounded-full flex items-center justify-center group">
  <% if content %>
    <%= content %>
  <% else %>
    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-8 h-8 text-white group-hover:rotate-90 duration-300">
      <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
    </svg>
  <% end %>
</div>
