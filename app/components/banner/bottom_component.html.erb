<div data-controller="banner--bottom-component" class="fixed bottom-0 left-0 z-50 flex justify-between w-full p-4 border-b border-gray-200 bg-gray-50 dark:bg-gray-700 dark:border-gray-600">
    <div class="flex items-center mx-auto">
      <%= content? ? content : "Content of the Bottom Banner!" %>
    </div>
    <%= render(Button::CloseComponent.new) %>
</div>